using UnityEngine;
using UnityEngine.UI;

public class BetIcon : MonoBehaviour
{
    [SerializeField] private string _iconName;
    [SerializeField] private Image _iconNameText;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
